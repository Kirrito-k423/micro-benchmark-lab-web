# 原型设计记录

问题：AKL 的接口性能测量，应该以曲线、数据表，还是实验报告作为首要入口？

三个候选共用一份 A3/A5 DataCopy 数据与交互状态，结构不同。用户反馈「方案 A 不错」，当前沿 A 继续迭代，尚未进入正式版实现。

初步建议：日常优化从 A 入手，批量核对采用 B，公开归档采用 C；B/C 暂保留作为比较参考。

实现边界：只读、内存状态、单页静态数据、独立原型分支，无后台、账户、持久化和自动上传。正式版应先确定数据契约与结果审核流程，再实现结果导入、版本索引和 URL 完整筛选分享。

数据核对：两组 A5 回传 ZIP 的顺序拼接、声明 MD5、CRC、AKL 原始记录验证通过；A3 CSV 与 A5 summary 的 1900 行 p50/p95/有效吞吐，从完整的 38000 个 raw tick 重新计算一致。并非重新进行 NPU 实验。

浏览器验收：A/B/C 三个布局已查看；方向与工作集切换、表格排序、shape 搜索、无匹配状态、键盘清空、曲线点选与原始样本联动、大 shape 缩放均已操作确认。构建通过；未编写原型测试套件。原型保存为本次设计讨论的主要证据，保留到设计决策完成后；不直接合入 main。

交互修复：用户发现下拉选择失效、点击后跳回页顶。根因为方案按钮选择器误匹配带 `data-variant` 的 `body`，所有点击冒泡后都触发页面重建与回顶。绑定收窄到按钮，重建时保留滚动、焦点、表格横向位置和展开状态。复验采用真实点击展开并选择工作集与 batch；页面中部排序和点选行前后 `scrollY` 均为 794，详情同步更新。此前通过直接赋值式选择操作的检查未覆盖这个冒泡问题。
